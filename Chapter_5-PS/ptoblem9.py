# Can you change the values inside a list which is contained in set S?
# s = {8, 7, 12, "Harry", [1,2]}

s = {8, 7, 12, "Harry", [1,2]}
# set can only contain immutable element, but list is mutable, so the set 's' is invalid.
# No, we cant change the values inside a list which is contained in set 's'.
