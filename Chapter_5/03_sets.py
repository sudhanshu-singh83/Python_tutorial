r = set() # we use set() normal bracket to print empty set, if we use curly bracket {} then it will consider it as dict.
print(type(r))

s = {1, 9, 3, 46, 26, 2, 2, 2} # set doesn't repeat eliments, so it returns 2 only 1 time
print(s)